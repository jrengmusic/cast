diff --git a/modules/juce_gui_basics/components/juce_Component.cpp b/modules/juce_gui_basics/components/juce_Component.cpp
index 1a6dfc60e6..d5fd0e49eb 100644
--- a/modules/juce_gui_basics/components/juce_Component.cpp
+++ b/modules/juce_gui_basics/components/juce_Component.cpp
@@ -845,11 +845,15 @@ void Component::setBufferedToImage (bool shouldBeBuffered)
 
     jassert (componentData == nullptr
              || componentData->cachedImage.get() == nullptr
-             || dynamic_cast<detail::StandardCachedComponentImage*> (componentData->cachedImage.get()) != nullptr);
+             || dynamic_cast<detail::StandardCachedComponentImage*> (componentData->cachedImage.get()) != nullptr
+             || externalCachedImageFactory != nullptr);
 
     if (shouldBeBuffered)
     {
-        createDataIfNeeded().cachedImage = std::make_unique<detail::StandardCachedComponentImage> (*this);
+        if (externalCachedImageFactory != nullptr)
+            createDataIfNeeded().cachedImage = externalCachedImageFactory (*this);
+        else
+            createDataIfNeeded().cachedImage = std::make_unique<detail::StandardCachedComponentImage> (*this);
     }
     else if (componentData != nullptr)
     {
diff --git a/modules/juce_gui_basics/components/juce_Component.h b/modules/juce_gui_basics/components/juce_Component.h
index 03dceeab43..1b7a93a479 100644
--- a/modules/juce_gui_basics/components/juce_Component.h
+++ b/modules/juce_gui_basics/components/juce_Component.h
@@ -2510,6 +2510,12 @@ public:
     */
     CachedComponentImage* getCachedComponentImage() const noexcept;
 
+    /** A function pointer that, if set, allows an external module to provide a custom
+        CachedComponentImage for setBufferedToImage. If null, the standard cached image
+        is used.
+    */
+    static inline std::unique_ptr<CachedComponentImage> (*externalCachedImageFactory) (Component&) = nullptr;
+
     /** Invalidates cached images, both in the CachedComponentImage (if any) and the image effect state. */
     void invalidateCachedImageResources();
 
