diff --git a/modules/juce_gui_basics/native/juce_NSViewComponentPeer_mac.mm b/modules/juce_gui_basics/native/juce_NSViewComponentPeer_mac.mm
index 4eb8addb95..007736f4f0 100644
--- a/modules/juce_gui_basics/native/juce_NSViewComponentPeer_mac.mm
+++ b/modules/juce_gui_basics/native/juce_NSViewComponentPeer_mac.mm
@@ -1030,6 +1030,10 @@ public:
 
     void renderRect (CGContextRef cg, NSRect r, float displayScale)
     {
+        if (externalContextFactory != nullptr)
+            if (auto ctx = externalContextFactory (*this))
+                { handlePaint (*ctx); return; }
+
        #if USE_COREGRAPHICS_RENDERING
         if (usingCoreGraphics)
         {
diff --git a/modules/juce_gui_basics/native/juce_Windowing_linux.cpp b/modules/juce_gui_basics/native/juce_Windowing_linux.cpp
index de5fef5585..6403a65253 100644
--- a/modules/juce_gui_basics/native/juce_Windowing_linux.cpp
+++ b/modules/juce_gui_basics/native/juce_Windowing_linux.cpp
@@ -502,6 +502,16 @@ private:
 
             if (! totalArea.isEmpty())
             {
+                if (ComponentPeer::externalContextFactory != nullptr)
+                {
+                    if (auto ctx = ComponentPeer::externalContextFactory (peer))
+                    {
+                        peer.handlePaint (*ctx);
+                        lastTimeImageUsed = Time::getApproximateMillisecondCounter();
+                        return;
+                    }
+                }
+
                 const auto wasImageNull = image.isNull();
 
                 if (wasImageNull || image.getWidth() < totalArea.getWidth()
diff --git a/modules/juce_gui_basics/native/juce_Windowing_windows.cpp b/modules/juce_gui_basics/native/juce_Windowing_windows.cpp
index a5d39ecd19..31a62704ec 100644
--- a/modules/juce_gui_basics/native/juce_Windowing_windows.cpp
+++ b/modules/juce_gui_basics/native/juce_Windowing_windows.cpp
@@ -2456,6 +2456,17 @@ private:
     //==============================================================================
     void handlePaintMessage()
     {
+        if (externalContextFactory != nullptr)
+        {
+            PAINTSTRUCT ps;
+            BeginPaint (hwnd, &ps);
+            if (auto ctx = externalContextFactory (*this))
+                handlePaint (*ctx);
+            EndPaint (hwnd, &ps);
+            lastPaintTime = Time::getMillisecondCounter();
+            return;
+        }
+
         if (renderContext != nullptr)
             renderContext->handlePaintMessage();
 
diff --git a/modules/juce_gui_basics/windows/juce_ComponentPeer.h b/modules/juce_gui_basics/windows/juce_ComponentPeer.h
index 1ac1523d44..50f859fee5 100644
--- a/modules/juce_gui_basics/windows/juce_ComponentPeer.h
+++ b/modules/juce_gui_basics/windows/juce_ComponentPeer.h
@@ -483,6 +483,12 @@ public:
     virtual int getCurrentRenderingEngine() const;
     virtual void setCurrentRenderingEngine (int index);
 
+    /** A function pointer that, if set, allows an external module to provide a custom
+        LowLevelGraphicsContext for component painting. If the factory returns nullptr,
+        the peer falls back to its default rendering engine.
+    */
+    static inline std::unique_ptr<LowLevelGraphicsContext> (*externalContextFactory) (ComponentPeer&) = nullptr;
+
     //==============================================================================
     /** On desktop platforms this method will check all the mouse and key states and return
         a ModifierKeys object representing them.
